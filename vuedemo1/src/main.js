import Vue from 'vue'
import App from './App.vue'
// import { marked } from 'marked'
// import { debounce } from 'lodash-es'

// Vue.use(marked);
// Vue.use(debounce);
new Vue({
  el: '#app',
  render: h => h(App)
})
